# Project AGENTS Instructions

- Use the repository's canonical tooling: `npm`.
- Prefer targeted checks before broader runs; use `npm run build` and `npm run check` for site verification.
- Store project documentation under `./docs/`.
- Store implementation plans under `./docs/plans/`.
- Do not add new top-level documentation directories outside `./docs/`.
