---
import { contactLinks, type ContactLinkKey } from '../data/contactLinks';

const contactActionOrder: ContactLinkKey[] = ['email', 'github', 'linkedin'];
const contactActions = contactActionOrder.map((linkKey) => ({
	...contactLinks[linkKey],
	featured: linkKey === 'email',
}));
---

<section
	id="contact"
	class="relative isolate overflow-hidden rounded-[2.5rem] border border-[color:var(--color-border)] bg-[color:color-mix(in_srgb,var(--color-surface)_76%,transparent)] px-6 py-8 shadow-[0_28px_90px_rgba(4,19,24,0.2)] scroll-mt-28 sm:px-8 sm:py-10 lg:px-10 lg:py-12"
	data-animate="fade-up"
	aria-labelledby="contact-heading"
>
	<div aria-hidden="true" class="absolute inset-0">
		<div class="absolute inset-0 bg-[linear-gradient(135deg,color-mix(in_srgb,var(--color-surface)_94%,transparent),color-mix(in_srgb,var(--color-background)_88%,transparent))]"></div>
		<div class="absolute inset-0 bg-[radial-gradient(circle_at_16%_18%,rgba(94,234,212,0.12),transparent_24%),radial-gradient(circle_at_84%_16%,rgba(45,212,191,0.08),transparent_22%)]"></div>
		<div class="absolute inset-x-0 top-0 h-px bg-[linear-gradient(90deg,transparent,rgba(94,234,212,0.42),transparent)]"></div>
		<div class="absolute inset-y-10 right-[34%] hidden w-px bg-[linear-gradient(180deg,transparent,rgba(148,163,184,0.16),transparent)] lg:block"></div>
	</div>

	<div class="relative z-10 grid gap-8 lg:grid-cols-[minmax(0,1fr)_minmax(19rem,0.82fr)] lg:items-end">
		<div class="space-y-5">
			<p class="text-xs font-semibold uppercase tracking-[0.32em] text-[color:var(--color-accent)]">
				Contact
			</p>
			<h2
				id="contact-heading"
				class="max-w-3xl text-3xl font-semibold tracking-[-0.04em] text-[color:var(--color-text)] sm:text-4xl lg:text-5xl"
			>
				Let&apos;s make the next interface feel clear, sharp, and ready to ship.
			</h2>
			<p class="max-w-2xl text-sm leading-7 text-[color:var(--color-text)] sm:text-base">
				If you want to talk through product direction, front-end craft, or a collaboration
				that needs both, I&apos;m easy to reach.
			</p>
			<p class="max-w-2xl text-sm leading-7 text-[color:var(--color-muted-text)] sm:text-base">
				Email is the fastest path. GitHub and LinkedIn are here if you want to see work,
				context, or connect there first.
			</p>
		</div>

		<div class="rounded-[2rem] border border-[color:color-mix(in_srgb,var(--color-accent)_22%,var(--color-border))] bg-[color:color-mix(in_srgb,var(--color-background)_24%,transparent)] p-5 backdrop-blur-sm sm:p-6">
			<div class="space-y-4">
				<p class="text-xs font-semibold uppercase tracking-[0.32em] text-[color:var(--color-accent)]">
					Reach out
				</p>

				<div class="grid gap-3 sm:grid-cols-3 lg:grid-cols-1">
					{
						contactActions.map(({ href, label, detail, external, featured }) => (
							<a
								href={href}
								target={external ? '_blank' : undefined}
								rel={external ? 'noreferrer' : undefined}
								class:list={[
									'group flex min-h-28 flex-col justify-between rounded-[1.5rem] border px-4 py-4 text-left transition duration-300 ease-out hover:-translate-y-0.5',
									featured
										? 'border-[color:var(--color-accent)] bg-[color:var(--color-accent)] text-[color:var(--color-background)] hover:border-[color:var(--color-accent-hover)] hover:bg-[color:var(--color-accent-hover)]'
										: 'border-[color:var(--color-border)] bg-[color:color-mix(in_srgb,var(--color-surface)_74%,transparent)] text-[color:var(--color-text)] hover:border-[color:var(--color-accent)] hover:text-[color:var(--color-accent)]',
								]}
							>
								<div class="flex items-center justify-between gap-3">
									<span class="text-sm font-semibold">{label}</span>
									<span
										class:list={[
											'rounded-full border px-2.5 py-1 text-[0.68rem] font-semibold uppercase tracking-[0.24em]',
											featured
												? 'border-[color:rgba(7,20,25,0.18)] bg-[color:rgba(7,20,25,0.08)] text-[color:var(--color-background)]'
												: 'border-[color:var(--color-border)] bg-[color:color-mix(in_srgb,var(--color-background)_18%,transparent)] text-[color:var(--color-muted-text)] group-hover:text-[color:var(--color-accent)]',
										]}
									>
										{external ? 'Open' : 'Send'}
									</span>
								</div>

								<p
									class:list={[
										'mt-4 text-sm leading-6',
										featured
											? 'text-[color:var(--color-background)]'
											: 'text-[color:var(--color-muted-text)] group-hover:text-[color:var(--color-text)]',
									]}
								>
									{detail}
								</p>
							</a>
						))
					}
				</div>
			</div>
		</div>
	</div>
</section>
